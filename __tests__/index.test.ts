import NftMarketplaceSdk from "../src/HyperSdk";

describe('HyperPlaza SDK', () => {
    it("should properly initialize the SDK", () => {
        const instance = new NftMarketplaceSdk('a', 'a', 'a');
    });
});