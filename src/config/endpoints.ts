import { HyperEndpoints } from "src/interfaces";

const hyprEndpoints: HyperEndpoints = {
  item: `/api/v1/tenant/items/`,
  currency: `/api/v1/tenant/currencies`,
  collection: `/api/v1/tenant/collections`
}

export default hyprEndpoints;